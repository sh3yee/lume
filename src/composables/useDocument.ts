/**
 * @deprecated 新代码请使用 `features/documents/model/documentStore`。
 * 此文件暂时保留为现有组件的兼容出口。
 */
export {
  useDocument,
  type CursorPosition,
  type DocumentDropPosition,
  type DocumentStats,
  type OpenDocument,
} from '@/features/documents/model/useDocument'
